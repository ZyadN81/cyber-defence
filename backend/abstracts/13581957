"In typical applications of homomorphic encryption, the first step consists for Alice of encrypting some plaintext m under Bob’s public key $$\\mathsf {pk}$$pk and of sending the ciphertext $$c = \\mathsf {HE}_{\\mathsf {pk}}(m)$$c=HEpk(m) to some third-party evaluator Charlie. This paper specifically considers that first step, i.e., the problem of transmitting c as efficiently as possible from Alice to Charlie. As others suggested before, a form of compression is achieved using hybrid encryption. Given a symmetric encryption scheme $$\\mathsf {E}$$E, Alice picks a random key k and sends a much smaller ciphertext $$c' = (\\mathsf {HE}_{\\mathsf {pk}}(k), \\mathsf {E}_k(m))$$c′=(HEpk(k),Ek(m)) that Charlie decompresses homomorphically into the original c using a decryption circuit $$\\mathcal {C}_{{\\mathsf {E}
