"Due to a production error, the following sentences were rendered incorrectly: the second sentence in Lemma 2 should have read: “Further, let m = ∑ logk m i=0 bi ki be the representation of m in base k, and let wk(m) denote the integer sum of all base k digits of m”; the last sentence of the second paragraph of Sect. 4.2.4 should have read: “If the available bandwidth in the transport stream is less than 2 Mbps, then the video streams in the transport stream can be compressed more, causing a slight reduction in their quality.” Further, the first sentence after Corollary 1 should have read: “Measured in the total number ofmessages (with amessage being either: a content segment variant, a tracing message or a revocation message), the computational cost for the head-end system and the bandwidth usage of Step 2 are O(p2 log n).”"
