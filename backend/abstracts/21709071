"The r-round (iterated) Even–Mansour cipher (also known as key-alternating cipher) defines a block cipher from r fixed public n-bit permutations $$P_1,\\ldots ,P_r$$ P1,…,Pr as follows: Given a sequence of n-bit round keys $$k_0,\\ldots ,k_r$$ k0,…,kr , an n-bit plaintext x is encrypted by xoring round key $$k_0$$ k0 , applying permutation $$P_1$$ P1 , xoring round key $$k_1$$ k1 , etc. The (strong) pseudorandomness of this construction in the random permutation model (i.e., when the permutations $$P_1,\\ldots ,P_r$$ P1,…,Pr are public random permutation oracles that the adversary can query in a black-box way) was studied in a number of recent papers, culminating with the work of Chen and Steinberger (EUROCRYPT 2014), who proved that the r-round Even–Mansour cipher is indistinguishable from a truly random permutation up to $$\\mathcal {O}(2
